{
  "name": "restful-booker-k6-perf-framework",
  "version": "0.1.0",
  "private": true,
  "description": "Enterprise-grade API performance testing framework for Restful-Booker using k6 and vanilla ES6 JavaScript.",
  "scripts": {
    "setup": "mkdir reports",
    "test:smoke": "k6 run src/tests/smoke.test.js -e TEST_NAME=smoke-local",
    "test:load": "k6 run src/tests/load.test.js -e TEST_NAME=load-local",
    "test:crud": "k6 run src/tests/booking.test.js -e TARGET_ENV=production -e TEST_NAME=crud-local"
  }
}
